#pragma once

#include "../environment.h"
#include "../primitives.h"

int main(){
	//nothing. just wanted to test these included files
	printASTToStdout(NULL, printASTToStdout(NULL, NIL));
	return 0;
}